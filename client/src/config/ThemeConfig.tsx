import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    background: {
      default: "#5025CA",
    },
    primary: {
      main: "#5025CA",
    },
    secondary: {
      main: "#FFFFFF",
    },
  },
});

export default theme;
